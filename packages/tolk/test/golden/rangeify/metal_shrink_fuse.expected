#include <metal_stdlib>
using namespace metal;
kernel void E_n2(device float* data0_1, device float* data1_16, uint3 gid [[threadgroup_position_in_grid]], uint3 lid [[thread_position_in_threadgroup]]) {
  float val0 = (*(data1_16+0));
  *(data0_1+0) = (0.0f*val0);
}
