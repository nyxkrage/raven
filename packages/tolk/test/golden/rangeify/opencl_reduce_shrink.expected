__kernel void r_16_16_2n2(__global float* data0_16, __global float* data1_1024, __global float* data2_16) {
  __attribute__ ((aligned (16))) __local float temp0[16];
  float acc0[1];
  float acc1[1];
  int gidx0 = get_group_id(0); /* 16 */
  int lidx0 = get_local_id(0); /* 16 */
  *(acc1+0) = 0.0f;
  for (int Ridx0 = 0; Ridx0 < 2; Ridx0++) {
    float val0 = (*(data1_1024+((lidx0<<1)+Ridx0+(gidx0<<5))));
    *(acc1+0) = ((*(acc1+0))+val0);
  }
  barrier(CLK_LOCAL_MEM_FENCE);
  *(temp0+lidx0) = (*(acc1+0));
  *(acc0+0) = 0.0f;
  for (int Ridx102 = 0; Ridx102 < 16; Ridx102++) {
    *(acc0+0) = ((*(acc0+0))+(temp0+Ridx102));
  }
  float val1 = (*(data2_16+gidx0));
  *(data0_16+gidx0) = ((*(acc0+0))+val1);
}
