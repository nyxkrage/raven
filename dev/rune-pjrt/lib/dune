(library
 (name rune_pjrt)
 (public_name rune-pjrt)
 (private_modules triton_dsl)
 (libraries nx nx.core nx.buffer nx.effect nx.backend unix)
 (foreign_stubs
  (language c)
  (names rune_pjrt_stubs)
  (flags
   (:standard
    (:include c_flags.sexp))))
 (c_library_flags
  (:include c_library_flags.sexp)))

(rule
 (targets c_flags.sexp c_library_flags.sexp)
 (action
  (run config/discover.exe)))
